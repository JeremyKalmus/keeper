# Auth Seed Registry
# Machine-readable registry of approved auth patterns
# Keeper uses this to enforce consistent identity and authorization

auth_model:
  type: none  # discovered - no auth implemented yet
  notes: "Dashboard is local dev tool - no auth required"
  future_considerations:
    - API key for production deployment
    - OAuth for team dashboard sharing

cors:  # discovered
  allowed_origins: ["*"]
  methods: ["GET", "OPTIONS"]
  headers: ["Content-Type"]
  credentials: false

security_headers:  # discovered
  implemented:
    - Cache-Control: no-cache (SSE)
    - Connection: keep-alive (SSE)
  not_implemented:
    - CSP
    - X-Frame-Options
    - X-Content-Type-Options
  notes: "Minimal headers for local dev tool"

# Placeholder for future auth patterns
scopes: {}
roles: {}
token_shape: {}
